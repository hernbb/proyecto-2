

<div class="nameProfile marginUpdate">
    <div>
      <h2>Bienvenido {{user.username}}</h2>
      <br>
      <h5><b>{{user.name}}</b></h5>
    </div>
    <div>
  <a class="btn btn-sm btn-dark btn-block mb-1 buttonBackground gold" href="/update-profile/{{user._id}}">Update profile</a>
  <a class="btn btn-sm btn-dark btn-block mb-1 buttonBackground gold" href="/logout">Logout</a>
    </div>
</div>

<div id="userTagInfo">
  <div class="disFlex">
    <div class="userTag">
      {{#if userTag}}
      <h5 class="center" >Clash User: <b>{{userTag.name}}</b></h5>
      <div  class="userName">
      <p>Arena:   <b>{{userTag.arena.name}}</b></p>
      <p>Clan:   <b>{{userTag.clan.name}}</b></p>
      <p>Exp Level:   <b>{{userTag.expLevel}}</b></p>
      <p>Trophies:   <b>{{userTag.trophies}}</b></p>
      <p>Wins:   <b>{{userTag.wins}}</b></p>
      <p>Losses:   <b>{{userTag.losses}}</b></p>
      <p>Win ratio: <b>{{avg}}</b></p>
      </div>
    </div>
    <div class="center">
      <h5>Favorite card</h5>
      <img src="{{userTag.currentFavouriteCard.iconUrls.medium}}" class="img"></img> 
      <p>{{userTag.currentFavouriteCard.name}}</p>
    </div>
  <div>
      <h4 class="center">Current Deck</h4>
  <div id="deck">
      {{#each userTag.currentDeck}}
      <div class="card">
      <img src="{{iconUrls.medium}}" class="img"></img> 
      <p>{{name}}</p>
      </div>
      {{/each}}

  </div>
  </div>

    {{!-- lista chest --}}
  <div> 
    <div class="listaChest centrado">
      <h5 class="center ">Upcomming chests:</h5>
        <ol>
            {{#each chest}}
            <li>{{name}}</li>
            {{!-- <img src="{{iconUrls}}" class="imgChest" chest="{{name}}"></img>  --}}
            {{/each}}
        </ol>
    </div>
  </div>
  </div>
      {{/if}}
  
  
</div>

</b>
<br>
<br>
<br>
<br>
<br>
<br>
{{!-- {{#each user.favorites}}
<img src={{iconUrls}} alt={{name}}>
<p>{{name}}</p>
<form action = "/delete-favorite" method="POST">
    <input type="hidden" name="id" value={{_id}}>

<button type="submit">borrar favorito</button>
</form>
{{/each}} --}}
<h3 class="center"> Favorite cards ❤️</h3> 
<div id="favorite">
{{#each user.favorites}}
<div id="favoriteCards" class="card">
  <div  class="bg-image hover-overlay ripple " data-mdb-ripple-color="light">
    <img src={{iconUrls}} alt={{name}} class="img-fluid"/>
   
  </div>
  <div class="card-body center">
    <h5 class="card-title">{{name}}</h5>
    
    <form action = "/delete-favorite" method="POST">
    <input type="hidden" name="id" value={{_id}}>
    <button type="submit" class="btn btn-sm buttonBackground btn-dark gold">borrar favorito</button>
    </form>

    {{!-- <form action = "/deck" method="POST">
    <input type="hidden" name="id" value={{_id}}>
    <button type="submit" class="btn btn-primary">Add to deck</button>
    </form> --}}
  </div>
</div>
{{/each}}
</div>